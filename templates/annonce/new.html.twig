{% extends 'base.html.twig' %}

{% block title %}Créer une annonce{% endblock %}

{% block body %}

    {# Messages flash #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="my-4 px-4 py-3 rounded
                {% if label == 'success' %} bg-green-100 border border-green-300 text-green-800
                {% elseif label == 'error' %} bg-red-100 border border-red-300 text-red-800
                {% endif %}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

    <div class="max-w-xl mx-auto p-6">
        <div class="text-center mb-10">
            <h1 class="text-3xl font-bold text-gray-900">
                Créez votre annonce<br>
                <span class="text-blue-600">en toute simplicité</span>
            </h1>
        </div>

        {{ form_start(form, {'attr': {
            'class': 'space-y-6',
            'enctype': 'multipart/form-data'
        }}) }}
        {{ form_errors(form) }}

        <div class="flex flex-col gap-2">
            <label class="font-semibold text-sm text-gray-700">Titre</label>
            {{ form_widget(form.titre, {'attr': {
                'placeholder': "Ex : Donne cours de maths",
                'class': 'px-4 py-2 rounded-lg border border-gray-300 bg-gray-100 focus:ring-2 focus:ring-blue-500 text-sm'
            }}) }}
            {{ form_errors(form.titre) }}
        </div>

        <div class="flex flex-col gap-2">
            <label class="font-semibold text-sm text-gray-700">Description</label>
            {{ form_widget(form.description, {'attr': {
                'placeholder': "Décrivez votre annonce...",
                'class': 'px-4 py-2 rounded-lg border border-gray-300 bg-gray-100 focus:ring-2 focus:ring-blue-500 text-sm'
            }}) }}
            {{ form_errors(form.description) }}
        </div>

        <div class="flex flex-col gap-2">
            <label class="font-semibold text-sm text-gray-700">Ville</label>
            {{ form_widget(form.ville, {'attr': {
                'placeholder': 'Ex : Paris',
                'class': 'px-4 py-2 rounded-lg border border-gray-300 bg-gray-100 focus:ring-2 focus:ring-blue-500 text-sm'
            }}) }}
            {{ form_errors(form.ville) }}
        </div>

        <div class="flex flex-col gap-2">
            <label class="font-semibold text-sm text-gray-700">Catégorie</label>
            {{ form_widget(form.category, {'attr': {
                'class': 'px-4 py-2 rounded-lg border border-gray-300 bg-white focus:ring-2 focus:ring-blue-500 text-sm'
            }}) }}
            {{ form_errors(form.category) }}
        </div>

        {# --------- PHOTOS MULTIPLES --------- #}
        <div class="flex flex-col gap-2">
            <label class="font-semibold text-sm text-gray-700">Photos</label>
            {{ form_widget(form.photos, {'attr': {
                'multiple': true,
                'class': 'block w-full text-sm text-gray-700 border border-gray-300 rounded-lg bg-white p-2'
            }}) }}
            {{ form_errors(form.photos) }}
            <p class="text-xs text-gray-500">Après enregistrement, vous pourrez choisir la photo principale.</p>
        </div>

        {# --------- (Optionnel) Photos déjà associées si on renvoie annonce ici --------- #}
        {% if annonce is defined and annonce.photos|length > 0 %}
            <div class="mt-6">
                <h2 class="text-sm font-semibold text-gray-700 mb-2">Choisir la photo principale</h2>
                <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
                    {% for photo in annonce.photos %}
                        <label class="block border rounded-lg overflow-hidden cursor-pointer">
                            <img src="{{ asset('uploads/photos/' ~ photo.filename) }}" alt="photo" class="h-32 w-full object-cover">
                            <div class="p-2 flex items-center gap-2">
                                <input type="radio" name="mainPhoto" value="{{ photo.id }}" {% if photo.isMain %}checked{% endif %}>
                                <span class="text-xs text-gray-700">Photo principale</span>
                                <a href="#">supprimer la photo</a>
                            </div>
                        </label>
                    {% endfor %}
                </div>
            </div>
        {% endif %}

        <button type="submit"
                class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded-lg text-center">
            Publier l'annonce
        </button>

        {{ form_end(form) }}
    </div>
{% endblock %}
